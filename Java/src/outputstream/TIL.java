package outputstream;

public class TIL {

	/*
	 바이트 단위 스트림 - OutputStream
	  - 바이트 단위 출력용 최상위 스트림
	  - 추상 메서드를 포함한 추상 클래스로 하위 클래스가 구현하여 사용한다
	  
	 flush()와 close()메서드
	  - 출력 버퍼를 비울때 flush() 메서드를 사용
	  - close()메서드 내부에서 flush()가 호출되므로 close()메서드가 호출되면 출력버퍼가 비워진다
	  
	 writer
	  - 문자 단위로 쓰는 최상위 스트림
	 */
}
